package seng202.team8.controller.gui;

/**
 * Controller for a basic GUI screen with no special attributes.
 */
public class BasicController extends GUIController {

    /**
     * Stores no info because the basic controller has no information to store.
     */
    @Override
    protected void storeInfo() {
       // Do nothing
    }
}
